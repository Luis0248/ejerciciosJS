/**
 * 1. Realizar un programa que pida al usuario 2 números y se realicen las 4 operaciones básicas
(Suma, resta, multiplicación, división). Imprimir los resultados.
 */

'use script';
let number1 = parseFloat(prompt("ingrese el primer numero"));
let numer2 = parseFloat(prompt("ingrese el segundo numero"));

let suma = number1 + numer2;
let resta = number1 - numer2;
let multiplicación = number1 * number2;
let división = number1 / numer2;
alert (`la suma es: ${suma}`);
alert ("resta", resta);
alert ("multiplicacion", multiplicación)
